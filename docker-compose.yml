version: "3.8"

services:
  tts:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./voice_profiles:/app/voice_profiles
      - ./audio_output:/app/audio_output
    restart: unless-stopped